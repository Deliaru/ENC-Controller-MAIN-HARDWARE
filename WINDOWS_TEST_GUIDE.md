# Windows环境测试指南

## 🎯 快速开始

### 方法1: 使用批处理文件 (推荐)
1. 双击运行 `run_test.bat`
2. 脚本会自动检查环境并安装依赖
3. 自动启动测试程序

### 方法2: 手动运行
1. 打开命令提示符 (以管理员身份运行)
2. 进入项目目录
3. 运行: `python test_simulation.py`

## 🔧 环境要求

### 必需软件
- **Python 3.6+**: [下载地址](https://www.python.org/downloads/)
- **pyusb库**: `pip install pyusb`

### 硬件要求
- ESP32设备已连接USB
- WinUSB驱动已正确安装

## 📋 安装步骤

### 1. 安装Python
1. 下载Python 3.6或更高版本
2. 安装时勾选"Add Python to PATH"
3. 验证安装: `python --version`

### 2. 安装pyusb
```cmd
pip install pyusb
```

### 3. 安装WinUSB驱动
如果设备显示"驱动程序未安装":
1. 运行 `install_driver.bat` (以管理员身份)
2. 或手动安装 `winusb.inf`

## 🚀 运行测试

### 1. 准备ESP32设备
```bash
# 编译并烧录测试程序
idf.py clean
idf.py build
idf.py flash monitor
```

### 2. 运行PC端测试
```cmd
# 方法1: 使用批处理文件
run_test.bat

# 方法2: 直接运行Python脚本
python test_simulation.py
```

## 📊 预期输出

### 成功连接时
```
🎮 ESP32 模拟数据测试工具 - Windows版本
============================================================
🔍 检查运行环境...
✅ pyusb库已安装
✅ libusb后端可用

============================================================
📋 预期数据模式分析:
按钮序列 (每秒切换):
   1. 左A    : 左=0x20, 右=0x00
   2. 左B    : 左=0x10, 右=0x00
   ...

============================================================
🔍 正在查找Ontroller设备...
✅ 找到Ontroller设备
设备信息: VID=0x0E8F, PID=0x1216
🔧 配置设备...
🔍 查找输入端点...
✅ 找到输入端点0x84

🎮 开始测试模拟数据...
[14:30:01] #   1 按钮: 左A           | 摇杆:   0.0° (-30000) | 原始: 44 44 54 20 00 34 00
[14:30:02] #  11 按钮: 左B           | 摇杆:  72.0° (-18000) | 原始: 44 44 54 10 00 B4 01
🔄 按钮切换: 左B
```

### 常见错误及解决方案

#### 1. 未找到设备
```
❌ 未找到Ontroller设备
💡 请确保:
   1. ESP32设备已连接
   2. WinUSB驱动已安装
   3. 设备VID=0x0E8F, PID=0x1216
```
**解决方案**:
- 检查USB连接
- 安装WinUSB驱动
- 确认设备VID/PID正确

#### 2. 权限不足
```
❌ 设备访问错误: [Errno 13] Permission denied
```
**解决方案**:
- 以管理员身份运行命令提示符
- 检查设备管理器中的设备状态

#### 3. 缺少pyusb库
```
❌ 缺少USB库，请安装: pip install pyusb
```
**解决方案**:
```cmd
pip install pyusb
```

#### 4. 端点未找到
```
❌ 未找到输入端点0x84
```
**解决方案**:
- 确认ESP32程序正确烧录
- 检查USB描述符配置
- 重新插拔设备

## 🔍 故障排除

### 检查设备状态
1. 打开设备管理器
2. 查找"ONTROLLER"设备
3. 确认设备状态正常 (无黄色感叹号)

### 检查驱动安装
```cmd
# 查看已安装的驱动
pnputil /enum-drivers | findstr winusb
```

### 重新安装驱动
```cmd
# 以管理员身份运行
install_driver.bat
```

### 检查Python环境
```cmd
# 检查Python版本
python --version

# 检查pyusb安装
python -c "import usb.core; print('pyusb OK')"
```

## 📈 测试验证

### 按钮测试验证
- 每秒应该看到按钮按顺序切换
- 按钮名称应该正确显示
- 原始数据应该符合预期格式

### 摇杆测试验证
- 摇杆值应该在5秒内从0°变化到360°
- 角度变化应该是线性的
- 原始值应该在-30000到+30000范围内

### 数据格式验证
- 头部始终为: `44 44 54`
- 数据包大小: 7字节
- 按钮掩码符合Ontroller.cs规范

## 💡 使用技巧

1. **实时监控**: 程序会实时显示数据变化
2. **统计信息**: 程序结束时显示总数据包数
3. **错误处理**: 自动处理USB超时和连接断开
4. **用户中断**: 按Ctrl+C可以安全停止测试

## 🎉 成功标志

当看到以下输出时，说明测试成功：
- ✅ 设备连接正常
- ✅ 数据格式正确
- ✅ 按钮按序切换
- ✅ 摇杆线性变化
- ✅ 无错误信息

**恭喜！您的ESP32设备与Ontroller.WinUSB.IO完全兼容！** 🎊 